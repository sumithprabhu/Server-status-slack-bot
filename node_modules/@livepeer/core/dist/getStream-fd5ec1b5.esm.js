import { g as getClient } from './client-8a4dc167.esm.js';

function getLivepeerProvider() {
  const client = getClient();
  return client.provider;
}

function getStream(args) {
  const provider = getLivepeerProvider();
  return provider.getStream(args);
}

export { getStream as a, getLivepeerProvider as g };
