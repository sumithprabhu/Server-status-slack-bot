/**
 * Checks if WebRTC is supported.
 */
export declare const isWebRTCSupported: () => boolean;
export declare function createPeerConnection(): RTCPeerConnection | null;
export type WebRTCVideoConfig = {
    /**
     * The timeout of the network requests made for the SDP negotiation, in ms.
     *
     * @default 10000
     */
    sdpTimeout?: number;
    /**
     * Disables the speedup/slowdown mechanic in WebRTC, to allow for non-distorted audio.
     */
    constant?: boolean;
};
/**
 * Performs the actual SDP exchange.
 *
 * 1. Sends the SDP offer to the server,
 * 2. Awaits the server's offer.
 *
 * SDP describes what kind of media we can send and how the server and client communicate.
 *
 * https://developer.mozilla.org/en-US/docs/Glossary/SDP
 * https://www.ietf.org/archive/id/draft-ietf-wish-whip-01.html#name-protocol-operation
 *
 * Returns the URL of the post-redirect media server such that we may DELETE our session later
 */
export declare function negotiateConnectionWithClientOffer(peerConnection: RTCPeerConnection | null | undefined, endpoint: string | null | undefined, ofr: RTCSessionDescription | null, config?: WebRTCVideoConfig): Promise<{
    playhead: Date;
    url: string;
}>;
/**
 * Constructs the client's SDP offer
 *
 * SDP describes what kind of media we can send and how the server and client communicate.
 *
 * https://developer.mozilla.org/en-US/docs/Glossary/SDP
 * https://www.ietf.org/archive/id/draft-ietf-wish-whip-01.html#name-protocol-operation
 */
export declare function constructClientOffer(peerConnection: RTCPeerConnection | null | undefined): Promise<RTCSessionDescription | null>;
