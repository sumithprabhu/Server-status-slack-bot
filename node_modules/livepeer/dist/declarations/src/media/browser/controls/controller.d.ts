import { ControlsOptions as ControlsOptionsBase, MediaControllerStore } from '@livepeer/core/media';
export type ControlsOptions = ControlsOptionsBase & {
    /** If hotkeys should be enabled on the media element (arrows to seek, etc) */
    hotkeys?: boolean;
};
export declare const addEventListeners: <TElement extends HTMLMediaElement, TMediaStream extends MediaStream>(store: MediaControllerStore<TElement, TMediaStream>, { hotkeys, autohide }?: ControlsOptions) => {
    destroy: () => void;
};
