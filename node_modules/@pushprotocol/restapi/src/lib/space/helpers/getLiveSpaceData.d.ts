import { EnvOptionsType, LiveSpaceData } from '../../types';
interface GetLatestMessageType extends EnvOptionsType {
    localAddress: string;
    spaceId: string;
    pgpPrivateKey: string;
}
declare const getLiveSpaceData: ({ localAddress, spaceId, pgpPrivateKey, env, }: GetLatestMessageType) => Promise<LiveSpaceData>;
export default getLiveSpaceData;
