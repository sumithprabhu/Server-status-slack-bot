import type Space from './Space';
export interface RejectPromotionInviteType {
    invitorAddress: string;
}
export declare function rejectPromotionInvite(this: Space, options: RejectPromotionInviteType): Promise<void>;
