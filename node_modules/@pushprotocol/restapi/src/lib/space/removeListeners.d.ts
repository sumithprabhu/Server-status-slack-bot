import { EnvOptionsType, SignerType, SpaceDTO } from '../types';
export interface RemoveListenersFromSpaceType extends EnvOptionsType {
    spaceId: string;
    listeners: Array<string>;
    account?: string;
    signer?: SignerType;
    pgpPrivateKey?: string;
}
export declare const removeListeners: (options: RemoveListenersFromSpaceType) => Promise<SpaceDTO>;
