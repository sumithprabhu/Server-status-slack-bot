import type Space from './Space';
import { SPACE_INVITE_ROLES } from '../payloads/constants';
export interface IAcceptPromotionRequestType {
    signalData: any;
    promoteeAddress: string;
    role: SPACE_INVITE_ROLES;
    spaceId: string;
}
export declare function acceptPromotionRequest(this: Space, options: IAcceptPromotionRequestType): Promise<void>;
