import { ENV } from '../constants';
import { SpaceDTO } from '../types';
/**
 *  POST /v1/spaces/search
 */
export interface SearchSpacesType {
    searchTerm: string;
    pageNumber: number;
    pageSize: number;
    env?: ENV;
}
export declare const search: (options: SearchSpacesType) => Promise<SpaceDTO[]>;
