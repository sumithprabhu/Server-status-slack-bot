import type Space from './Space';
export interface AcceptPromotionInviteType {
    signalData: any;
    invitorAddress: string;
    spaceId: string;
}
export declare function acceptPromotionInvite(this: Space, options: AcceptPromotionInviteType): Promise<void>;
