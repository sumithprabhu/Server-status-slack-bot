import { ENV } from '../constants';
import { SpaceIFeeds } from '../types';
export declare const spaceFeed: (options: {
    account: string;
    pgpPrivateKey?: string;
    toDecrypt?: boolean;
    env?: ENV;
    recipient: string;
}) => Promise<SpaceIFeeds>;
