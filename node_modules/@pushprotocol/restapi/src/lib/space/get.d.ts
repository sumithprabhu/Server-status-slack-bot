import { ENV } from '../constants';
import { SpaceDTO } from '../types';
export interface GetSpaceType {
    spaceId: string;
    env?: ENV;
}
export declare const get: (options: GetSpaceType) => Promise<SpaceDTO>;
