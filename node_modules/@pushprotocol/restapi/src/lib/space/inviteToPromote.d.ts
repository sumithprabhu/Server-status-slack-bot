import { SPACE_INVITE_ROLES } from '../payloads/constants';
import type Space from './Space';
export interface InviteToPromoteType {
    inviteeAddress: string;
    role: SPACE_INVITE_ROLES;
}
export declare function inviteToPromote(this: Space, options: InviteToPromoteType): Promise<void>;
