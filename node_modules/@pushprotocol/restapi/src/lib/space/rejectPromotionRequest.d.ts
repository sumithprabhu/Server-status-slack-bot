import type Space from './Space';
export interface RejectPromotionRequestType {
    promoteeAddress: string;
}
export declare function rejectPromotionRequest(this: Space, options: RejectPromotionRequestType): Promise<void>;
