import { SPACE_ACCEPT_REQUEST_TYPE, SPACE_DISCONNECT_TYPE, SPACE_REQUEST_TYPE, VIDEO_CALL_TYPE } from '../../payloads/constants';
import { EnvOptionsType, SignerType, VideoCallStatus, VideoNotificationRules } from '../../types';
interface CallDetailsType {
    type: SPACE_REQUEST_TYPE | SPACE_ACCEPT_REQUEST_TYPE | SPACE_DISCONNECT_TYPE;
    data: Record<string, unknown>;
}
export interface VideoDataType {
    /** @deprecated - Use `rules` object instead */
    chatId?: string;
    recipientAddress: string;
    senderAddress: string;
    signalData?: any;
    status: VideoCallStatus;
    callDetails?: CallDetailsType;
}
interface VideoCallInfoType extends VideoDataType, EnvOptionsType {
    callType?: VIDEO_CALL_TYPE;
    rules?: VideoNotificationRules;
}
interface UserInfoType {
    signer: SignerType;
    chainId: number;
    pgpPrivateKey: string;
}
declare const sendVideoCallNotification: ({ signer, chainId, pgpPrivateKey }: UserInfoType, { recipientAddress, senderAddress, chatId, rules, status, signalData, env, callType, callDetails, }: VideoCallInfoType) => Promise<void>;
export default sendVideoCallNotification;
