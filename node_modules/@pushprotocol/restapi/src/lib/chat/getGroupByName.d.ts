import { ENV } from '../constants';
import { GroupDTO } from '../types';
/**
 *  GET /v1/chat/groups/:chatId
 */
export interface GetGroupByNameType {
    groupName: string;
    env?: ENV;
}
export declare const getGroupByName: (options: GetGroupByNameType) => Promise<GroupDTO>;
