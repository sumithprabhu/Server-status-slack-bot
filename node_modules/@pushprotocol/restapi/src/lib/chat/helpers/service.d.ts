import { ENV } from '../../constants';
import { AccountEnvOptionsType, ConversationHashOptionsType, walletType } from '../../types';
type CreateUserOptionsType = {
    user: string;
    wallet?: walletType;
    publicKey?: string;
    encryptedPrivateKey?: string;
    env?: ENV;
    origin?: string | null;
};
export declare const createUserService: (options: CreateUserOptionsType) => Promise<import("../../types").IUser>;
export declare const authUpdateUserService: (options: CreateUserOptionsType) => Promise<import("../../types").IUser>;
export declare const getConversationHashService: (options: ConversationHashOptionsType) => Promise<{
    threadHash: string;
}>;
export interface GetMessagesOptionsType extends Omit<AccountEnvOptionsType, 'account'> {
    threadhash: string;
    limit: number;
}
export declare const getMessagesService: (options: GetMessagesOptionsType) => Promise<any>;
export {};
