import { ENV } from '../../constants';
/**
 *  GET /v1/chat/groups/:chatId
 */
export interface GetEncryptedSecret {
    sessionKey: string;
    env?: ENV;
}
export declare const getEncryptedSecret: (options: GetEncryptedSecret) => Promise<string>;
