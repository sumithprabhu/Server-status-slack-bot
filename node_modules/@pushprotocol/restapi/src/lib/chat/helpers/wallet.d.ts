import { SignerType, walletType } from '../../types';
export declare const getWallet: (options: walletType) => {
    account: string | null;
    signer: SignerType | null;
};
export declare const getAccountAddress: (options: walletType) => Promise<string>;
