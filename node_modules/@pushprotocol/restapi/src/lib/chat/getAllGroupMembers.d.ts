import { ChatMemberProfile, EnvOptionsType } from '../types';
export declare const getAllGroupMembers: (options: {
    chatId: string;
    env: EnvOptionsType['env'];
}) => Promise<ChatMemberProfile[]>;
