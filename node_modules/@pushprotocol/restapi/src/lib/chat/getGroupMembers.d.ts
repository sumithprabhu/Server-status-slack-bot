import { ENV } from '../constants';
import { ChatMemberProfile } from '../types';
/**
 * GET /v1/chat/:chatId/members
 */
export interface FetchChatGroupInfoType {
    chatId: string;
    page?: number;
    limit?: number;
    pending?: boolean;
    role?: string;
    env?: ENV;
}
export declare const getGroupMembers: (options: FetchChatGroupInfoType) => Promise<ChatMemberProfile[]>;
