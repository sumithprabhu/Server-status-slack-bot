import { ENV } from '../constants';
import { GroupDTO } from '../types';
/**
 *  GET /v1/chat/groups/:chatId
 */
export interface GetGroupType {
    chatId: string;
    env?: ENV;
}
export declare const getGroup: (options: GetGroupType) => Promise<GroupDTO>;
