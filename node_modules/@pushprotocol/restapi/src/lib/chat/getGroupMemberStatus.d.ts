import { ENV } from '../constants';
import { GroupMemberStatus } from '../types';
/**
 * GET /v1/chat/groups/:chatId/access/:did
 */
export interface GetGroupMemberStatusType {
    chatId: string;
    did: string;
    env?: ENV;
}
export declare const getGroupMemberStatus: (options: GetGroupMemberStatusType) => Promise<GroupMemberStatus>;
