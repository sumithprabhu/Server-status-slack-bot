import { ENV } from '../constants';
import { ChatMemberCounts } from '../types';
/**
 * GET /v1/chat/:chatId/members/count
 */
export interface FetchChatMemberCountType {
    chatId: string;
    env?: ENV;
}
export declare const getGroupMemberCount: (options: FetchChatMemberCountType) => Promise<ChatMemberCounts>;
