import { AccountEnvOptionsType, IMessageIPFS } from '../types';
import { IPGPHelper } from './helpers';
export interface HistoricalMessagesOptionsType extends AccountEnvOptionsType {
    threadhash: string;
    pgpPrivateKey?: string;
    /**
     * If true, the method will return decrypted message content in response
     */
    toDecrypt?: boolean;
    limit?: number;
}
/**
 * Get all the messages exchanged between users after the threadhash.
 */
export declare const history: (options: HistoricalMessagesOptionsType) => Promise<IMessageIPFS[]>;
export declare const historyCore: (options: HistoricalMessagesOptionsType, pgpHelper: IPGPHelper) => Promise<IMessageIPFS[]>;
