import { GroupInfoDTO } from '../types';
import { GetGroupType } from './getGroup';
/**
 *  GET /v2/chat/groups/:chatId
 */
export declare const getGroupInfo: (options: GetGroupType) => Promise<GroupInfoDTO>;
