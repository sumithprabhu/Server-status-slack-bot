import { ENV } from '../constants';
import { GroupDTO } from '../types';
/**
 *  POST /v1/chat/groups/search
 */
export interface SearchGroupsType {
    searchTerm: string;
    pageNumber: number;
    pageSize: number;
    env?: ENV;
}
export declare const search: (options: SearchGroupsType) => Promise<GroupDTO[]>;
