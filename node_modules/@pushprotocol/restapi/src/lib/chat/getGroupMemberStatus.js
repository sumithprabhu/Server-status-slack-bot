"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getGroupMemberStatus = void 0;
const tslib_1 = require("tslib");
const helpers_1 = require("../helpers");
const constants_1 = require("../constants");
const helpers_2 = require("./helpers");
const axiosUtil_1 = require("../utils/axiosUtil");
const getGroupMemberStatus = (options) => tslib_1.__awaiter(void 0, void 0, void 0, function* () {
    // Replace "any" with the actual response type
    const { chatId, did, env = constants_1.default.ENV.PROD } = options || {};
    try {
        if (chatId == null || chatId.length === 0) {
            throw new Error(`chatId cannot be null or empty`);
        }
        if (did == null || did.length === 0) {
            throw new Error(`did cannot be null or empty`);
        }
        const user = yield (0, helpers_2.getUserDID)(did, env);
        const API_BASE_URL = (0, helpers_1.getAPIBaseUrls)(env);
        const requestUrl = `${API_BASE_URL}/v1/chat/groups/${chatId}/members/${user}/status`;
        const response = yield (0, axiosUtil_1.axiosGet)(requestUrl);
        return response.data;
    }
    catch (err) {
        console.error(`[Push SDK] - API - Error - API ${exports.getGroupMemberStatus.name} -:  `, err);
        throw Error(`[Push SDK] - API - Error - API ${exports.getGroupMemberStatus.name} -: ${err}`);
    }
});
exports.getGroupMemberStatus = getGroupMemberStatus;
//# sourceMappingURL=getGroupMemberStatus.js.map