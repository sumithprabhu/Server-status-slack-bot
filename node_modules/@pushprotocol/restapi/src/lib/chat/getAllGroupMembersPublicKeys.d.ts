import { EnvOptionsType } from '../types';
export declare const getAllGroupMembersPublicKeys: (options: {
    chatId: string;
    env: EnvOptionsType['env'];
}) => Promise<{
    did: string;
    publicKey: string;
}[]>;
