/// <reference types="node" />
import { EventEmitter } from 'events';
import { ENV } from '../constants';
import { PushStreamInitializeProps, STREAM } from './pushStreamTypes';
import { ProgressHookType, SignerType } from '../types';
export declare class PushStream extends EventEmitter {
    private _listen;
    private decryptedPgpPvtKey?;
    private progressHook?;
    private signer?;
    private pushChatSocket;
    private pushNotificationSocket;
    private account;
    private raw;
    private options;
    private chatInstance;
    private listen;
    constructor(account: string, _listen: STREAM[], options: PushStreamInitializeProps, decryptedPgpPvtKey?: string | undefined, progressHook?: ((progress: ProgressHookType) => void) | undefined, signer?: SignerType | undefined);
    static initialize(account: string, listen: STREAM[], env: ENV, decryptedPgpPvtKey?: string, progressHook?: (progress: ProgressHookType) => void, signer?: SignerType, options?: PushStreamInitializeProps): Promise<PushStream>;
    connect(): Promise<void>;
    disconnect(): Promise<void>;
    private shouldEmitChat;
    private shouldEmitChannel;
    private shouldEmitVideo;
}
