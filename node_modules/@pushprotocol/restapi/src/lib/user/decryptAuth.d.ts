import { ENV } from '../constants';
import { ProgressHookType, SignerType } from '../types';
type decryptAuthProps = {
    signer: SignerType;
    account?: string;
    env?: ENV;
    additionalMeta?: {
        NFTPGP_V1?: {
            encryptedPassword: string;
        };
    };
    /**
     * To get Progress Related to fn
     */
    progressHook?: (progress: ProgressHookType) => void;
};
/**
 *
 * @returns Decrypted Push Profile Password
 */
export declare const decryptAuth: (options: decryptAuthProps) => Promise<string | null>;
export {};
