import { ENV, ENCRYPTION_TYPE } from '../constants';
import { SignerType, IUser, ProgressHookType } from '../types';
export type AuthUpdateProps = {
    pgpPrivateKey: string;
    pgpEncryptionVersion: ENCRYPTION_TYPE;
    signer: SignerType;
    pgpPublicKey: string;
    account?: string;
    env?: ENV;
    additionalMeta?: {
        NFTPGP_V1?: {
            password: string;
        };
    };
    progressHook?: (progress: ProgressHookType) => void;
};
/**
 * Updation of encryption keys of a Push Profile to a specific version
 */
export declare const authUpdate: (options: AuthUpdateProps) => Promise<IUser>;
