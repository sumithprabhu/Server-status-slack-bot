import { IUser, ProgressHookType, SignerType } from '../types';
import Constants, { ENV } from '../constants';
export type CreateUserPropsWithProfile = {
    env?: ENV;
    account?: string;
    signer?: SignerType;
    version?: typeof Constants.ENC_TYPE_V1 | typeof Constants.ENC_TYPE_V3;
    additionalMeta?: {
        NFTPGP_V1?: {
            password: string;
        };
    };
    profile?: {
        name?: string;
        desc?: string;
        picture?: string;
        blockedUsersList?: Array<string>;
    };
    progressHook?: (progress: ProgressHookType) => void;
};
export declare const createUserWithProfile: (userOptions: CreateUserPropsWithProfile) => Promise<IUser>;
