import { ENV } from '../constants';
export type FeedsPerChannelOptionsType = {
    user: string;
    env?: ENV;
    channels?: string[];
    page?: number;
    limit?: number;
    spam?: boolean;
    raw?: boolean;
};
export declare const getFeedsPerChannel: (options: FeedsPerChannelOptionsType) => Promise<any>;
