import { IUser } from '../types';
import { ENV } from '../constants';
export interface GetBatchType {
    userIds: string[];
    env?: ENV;
}
export declare const getBatch: (options: GetBatchType) => Promise<IUser>;
