import { ENV } from '../constants';
export type GetSubscribersOptionsType = {
    channel: string;
    env?: ENV;
};
export declare const _getSubscribers: (options: GetSubscribersOptionsType) => Promise<string[]>;
