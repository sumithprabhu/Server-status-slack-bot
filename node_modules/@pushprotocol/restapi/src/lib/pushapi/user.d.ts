import { ENV } from '../constants';
import { InfoOptions } from './pushAPITypes';
export declare class User {
    private account;
    private env;
    constructor(account: string, env: ENV);
    info(options?: InfoOptions): Promise<import("..").IUser>;
}
