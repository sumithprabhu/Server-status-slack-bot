import { ENV } from '../constants';
import { SignerType, VideoCallData } from '../types';
import { VideoV2 } from '../video/VideoV2';
import { VideoInitializeOptions } from './pushAPITypes';
export declare class Video {
    private account;
    private env;
    private decryptedPgpPvtKey?;
    private signer?;
    private peerInfos;
    constructor(account: string, env: ENV, decryptedPgpPvtKey?: string | undefined, signer?: SignerType | undefined);
    initialize(onChange: (fn: (data: VideoCallData) => VideoCallData) => void, options: VideoInitializeOptions): Promise<VideoV2>;
}
